<Window x:Class="MMH.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MMH"
        mc:Ignorable="d"
        Title="MMH" Height="435" Width="566">
    <ScrollViewer Margin="0,0,-29,-125">
        <StackPanel Orientation="Vertical" Margin="10">
            <!-- Replaced the simple Label with a styled Border + TextBlock that has gradient background,
     gradient text, drop shadow glow and a short entrance animation. Paste directly in place of the Label. -->
            <Border HorizontalAlignment="Center"
        CornerRadius="8"
        Padding="12,8"
        Margin="0,6,0,10"
        SnapsToDevicePixels="True">

                <Border.Effect>
                    <DropShadowEffect Color="#88000000" BlurRadius="10" ShadowDepth="3"/>
                </Border.Effect>

                <Grid>
                    <TextBlock Text="MMH (Multi Monitor Helper)"
                   FontFamily="Segoe UI Semibold"
                   FontSize="18"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   TextOptions.TextFormattingMode="Ideal"
                   TextOptions.TextRenderingMode="ClearType"
                   RenderTransformOrigin="0.5,0.5"
                   Opacity="0.98">
                        <TextBlock.Foreground>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#FFFFFFFF" Offset="0.0"/>
                                <GradientStop Color="#FFE6F7FF" Offset="1.0"/>
                            </LinearGradientBrush>
                        </TextBlock.Foreground>

            <!-- subtle glow (named so we can animate opacity) -->
                        <TextBlock.Effect>
                            <DropShadowEffect x:Name="titleGlow"
                                  Color="#FFFFFFFF"
                                  BlurRadius="12"
                                  ShadowDepth="0"
                                  Opacity="0.0"/>
                        </TextBlock.Effect>

                        <TextBlock.RenderTransform>
                            <ScaleTransform x:Name="titleScale" ScaleX="1" ScaleY="1"/>
                        </TextBlock.RenderTransform>
                    </TextBlock>

                    <Grid.Triggers>
                        <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                            <BeginStoryboard>
                                <Storyboard>
                                    <!-- pop / settle -->
                                    <DoubleAnimation Storyboard.TargetName="titleScale"
                                         Storyboard.TargetProperty="ScaleX"
                                         From="0.94" To="1.0"
                                         Duration="0:0:0.28"
                                         AccelerationRatio="0.2" DecelerationRatio="0.8"/>
                                    <DoubleAnimation Storyboard.TargetName="titleScale"
                                         Storyboard.TargetProperty="ScaleY"
                                         From="0.94" To="1.0"
                                         Duration="0:0:0.28"
                                         AccelerationRatio="0.2" DecelerationRatio="0.8"/>
                                    <!-- glow fade-in -->
                                    <DoubleAnimation Storyboard.TargetName="titleGlow"
                                         Storyboard.TargetProperty="Opacity"
                                         From="0.0" To="0.7"
                                         Duration="0:0:0.35"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Grid.Triggers>
                </Grid>
            </Border>

            <StackPanel Orientation="Horizontal" Margin="0,10,0,10" HorizontalAlignment="Stretch" >

                <!-- Profile 1 -->
                <GroupBox Margin="0,0,10,0" BorderBrush="#FFBBDDF0" Background="#FFF6FBFD">
                    <GroupBox.Header>
                        <Border Background="#FFDCEFF6" Padding="6">
                            <TextBlock Text="Profile 1" FontWeight="Bold"/>
                        </Border>
                    </GroupBox.Header>

                    <StackPanel Width="150" Margin="4">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,6,0,6">
                            <CheckBox x:Name="Profile1_Display1" Content="D1" Margin="2"/>
                            <CheckBox x:Name="Profile1_Display2" Content="D2" Margin="2"/>
                            <CheckBox x:Name="Profile1_Display3" Content="D3" Margin="2"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,4,0,4">
                            <TextBlock Text="Primary:" VerticalAlignment="Center" Margin="0,0,6,0" />
                            <RadioButton x:Name="Profile1_Primary1" GroupName="Profile1Primary" Content="1" Margin="2"/>
                            <RadioButton x:Name="Profile1_Primary2" GroupName="Profile1Primary" Content="2" Margin="2"/>
                            <RadioButton x:Name="Profile1_Primary3" GroupName="Profile1Primary" Content="3" Margin="2"/>
                        </StackPanel>

                        <TextBlock Text="Description:" Margin="2,4,2,0" FontSize="10"/>
                        <TextBox x:Name="Profile1_Description" Height="50" TextWrapping="Wrap" AcceptsReturn="True" Margin="2"/>

                        <Button Content="Apply" Click="ApplyProfile1_Click" Margin="0,6,0,0" />
                    </StackPanel>
                </GroupBox>

                <!-- Profile 2 -->
                <GroupBox Margin="0,0,10,0" BorderBrush="#FFBEE7C8" Background="#FFFBFEF6">
                    <GroupBox.Header>
                        <Border Background="#FFE8F7D9" Padding="6">
                            <TextBlock Text="Profile 2" FontWeight="Bold"/>
                        </Border>
                    </GroupBox.Header>

                    <StackPanel Width="150" Margin="4">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,6,0,6">
                            <CheckBox x:Name="Profile2_Display1" Content="D1" Margin="2"/>
                            <CheckBox x:Name="Profile2_Display2" Content="D2" Margin="2"/>
                            <CheckBox x:Name="Profile2_Display3" Content="D3" Margin="2"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,4,0,4">
                            <TextBlock Text="Primary:" VerticalAlignment="Center" Margin="0,0,6,0" />
                            <RadioButton x:Name="Profile2_Primary1" GroupName="Profile2Primary" Content="1" Margin="2"/>
                            <RadioButton x:Name="Profile2_Primary2" GroupName="Profile2Primary" Content="2" Margin="2"/>
                            <RadioButton x:Name="Profile2_Primary3" GroupName="Profile2Primary" Content="3" Margin="2"/>
                        </StackPanel>

                        <TextBlock Text="Description:" Margin="2,4,2,0" FontSize="10"/>
                        <TextBox x:Name="Profile2_Description" Height="50" TextWrapping="Wrap" AcceptsReturn="True" Margin="2"/>

                        <Button Content="Apply" Click="ApplyProfile2_Click" Margin="0,6,0,0" />
                    </StackPanel>
                </GroupBox>

                <!-- Profile 3 -->
                <GroupBox BorderBrush="#FFF0D1C4" Background="#FFFEFBF9">
                    <GroupBox.Header>
                        <Border Background="#FFF7DBD0" Padding="6">
                            <TextBlock Text="Profile 3" FontWeight="Bold"/>
                        </Border>
                    </GroupBox.Header>

                    <StackPanel Width="150" Margin="4">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,6,0,6">
                            <CheckBox x:Name="Profile3_Display1" Content="D1" Margin="2"/>
                            <CheckBox x:Name="Profile3_Display2" Content="D2" Margin="2"/>
                            <CheckBox x:Name="Profile3_Display3" Content="D3" Margin="2"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,4,0,4">
                            <TextBlock Text="Primary:" VerticalAlignment="Center" Margin="0,0,6,0" />
                            <RadioButton x:Name="Profile3_Primary1" GroupName="Profile3Primary" Content="1" Margin="2"/>
                            <RadioButton x:Name="Profile3_Primary2" GroupName="Profile3Primary" Content="2" Margin="2"/>
                            <RadioButton x:Name="Profile3_Primary3" GroupName="Profile3Primary" Content="3" Margin="2"/>
                        </StackPanel>

                        <TextBlock Text="Description:" Margin="2,4,2,0" FontSize="10"/>
                        <TextBox x:Name="Profile3_Description" Height="50" TextWrapping="Wrap" AcceptsReturn="True" Margin="2"/>

                        <Button Content="Apply" Click="ApplyProfile3_Click" Margin="0,6,0,0" />
                    </StackPanel>
                </GroupBox>

            </StackPanel>

            <Grid Margin="2,4,13,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Script Preview:" FontSize="10"/>
                <TextBox Grid.Row="1" x:Name="ScriptPreview" 
                         IsReadOnly="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         Height="110"
                         FontFamily="Consolas"
                         FontSize="12"/>

            </Grid>


        </StackPanel>
    </ScrollViewer>
</Window>
